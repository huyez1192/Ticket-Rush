import PagePlaceholder from "../PagePlaceholder";

export default function CheckoutSuccessPage() {
  return (
    <PagePlaceholder
      title="Checkout success"
      purpose="Derived post-checkout confirmation route."
      note="Phase 11 should decide whether this route redirects to ticket detail or remains a standalone success screen."
      status="Paid"
    />
  );
}
