import AdminPageShell from "./AdminPageShell";

export default function AdminDashboardPage() {
  return (
    <AdminPageShell
      title="Dashboard"
      purpose="Admin analytics dashboard shell for overview, revenue, occupancy, and demographics."
      note="Phase 13 will wire dashboard endpoints and keep live/time-series areas as placeholders where backend data is partial."
      status="Published"
    />
  );
}
