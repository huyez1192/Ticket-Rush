import PagePlaceholder from "../PagePlaceholder";

export default function EventListingPage() {
  return (
    <PagePlaceholder
      title="Events"
      purpose="Public event discovery route for browsing and filtering events."
      note="Phase 10 will replace this shell with the event card grid and supported Swagger filters."
      status="Published"
    />
  );
}
